using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class _집가고싶다 : MonoBehaviour
{
    public Transform _시바;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //_시바.transform
    }
}
